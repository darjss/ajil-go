apiVersion: v1
kind: Service
metadata:
  name: server
  namespace: ajil-go
  labels:
    app: server
    app.kubernetes.io/name: ajil-go
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app: server
  ports:
    - name: http
      port: 3001
      targetPort: 3001
      protocol: TCP
