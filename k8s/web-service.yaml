apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: ajil-go
  labels:
    app: web
    app.kubernetes.io/name: ajil-go
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  selector:
    app: web
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
